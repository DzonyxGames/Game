const cfg=window.DZONYX_CONFIG||{};["steamTop","steamHero","steamBottom"].forEach(id=>{const el=document.getElementById(id);if(!el)return;el.href=cfg.steamUrl||"#";el.addEventListener("click",e=>{if(!cfg.steamUrl||cfg.steamUrl==="#"){e.preventDefault();alert("Add your Steam store URL in config.js when the page is ready.")}})});const discord=document.getElementById("discordBtn");discord.href=cfg.discordUrl||"#";discord.addEventListener("click",e=>{if(!cfg.discordUrl||cfg.discordUrl==="#"){e.preventDefault();alert("Add your Discord invite in config.js.")}});const contact=document.getElementById("contactBtn");contact.href=cfg.contactUrl||"#";contact.addEventListener("click",e=>{if(!cfg.contactUrl||cfg.contactUrl==="#"){e.preventDefault();alert("Add your contact link or email in config.js.")}});document.getElementById("year").textContent=new Date().getFullYear();const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible");observer.unobserve(entry.target)}})},{threshold:.14});document.querySelectorAll(".reveal").forEach(el=>observer.observe(el));const glow=document.getElementById("cursorGlow");window.addEventListener("pointermove",e=>{glow.style.left=e.clientX+"px";glow.style.top=e.clientY+"px"});const modal=document.getElementById("shotModal"),modalTitle=document.getElementById("modalTitle");document.querySelectorAll(".shot").forEach(s=>s.addEventListener("click",()=>{modalTitle.textContent=s.dataset.shot;modal.classList.add("open")}));function closeModal(){modal.classList.remove("open")}document.getElementById("modalClose").addEventListener("click",closeModal);modal.addEventListener("click",e=>{if(e.target===modal)closeModal()});document.addEventListener("keydown",e=>{if(e.key==="Escape")closeModal()});